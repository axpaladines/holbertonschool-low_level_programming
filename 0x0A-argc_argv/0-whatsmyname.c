#include <stdio.h>

/**
* main - print the name
* @argc: number of input
* @argv: array
* Return: Always 0
*/

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
